---
phase: quick
plan: 260401-0ka
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [QUICK-README]

must_haves:
  truths:
    - "README.md exists at project root with light-hearted, engaging tone"
    - "README conveys what brain-cache does, why it exists, and how to use it"
    - "README includes install, quick-start, and CLI command reference"
  artifacts:
    - path: "README.md"
      provides: "Project README with personality"
      contains: "brain-cache"
  key_links: []
---

<objective>
Create a light-hearted, personality-rich README.md for the brain-cache project.

Purpose: Give the project a welcoming front door that explains what brain-cache does, why developers should care, and how to get started -- all without reading like a corporate whitepaper.
Output: README.md at project root
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@package.json
@src/cli/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with light-hearted tone</name>
  <files>README.md</files>
  <action>
Create README.md at project root with a warm, slightly irreverent developer tone. Use "brain-cache" (hyphenated) throughout per project naming convention.

Structure and content:

1. **Title and tagline** — `# brain-cache` with a one-liner that captures the value prop with personality. Something like "Your GPU has been sitting there doing nothing. Let's fix that." Avoid corporate-speak.

2. **What is this?** — 2-3 sentences explaining brain-cache: a local AI runtime that uses your GPU as a cache layer for Claude. Offloads embeddings, retrieval, and context preprocessing to Ollama so Claude only sees what matters. Frame it as "your local brain that pre-chews context so Claude doesn't have to."

3. **Why?** — Brief section on the problem: Claude is brilliant but expensive per-token. Sending your entire codebase as context is wasteful. brain-cache runs the grunt work locally (embeddings, search, ranking) and sends Claude a focused, minimal context window. Include a fun before/after comparison — e.g., "Before: shoving 200 files into Claude's context. After: sending 3 perfectly chosen snippets."

4. **Quick Start** — Numbered steps:
   - Prerequisites: Node.js 22+, Ollama installed and running
   - `npm install` (or clone + build)
   - `brain-cache init` (detects hardware, pulls embedding model)
   - `brain-cache index .` (indexes current directory)
   - `brain-cache ask "How does the auth middleware work?"` (ask a question)

5. **CLI Commands** — Table or list of all 6 commands from src/cli/index.ts:
   - `init` — Detect hardware, pull embedding model, create config
   - `doctor` — System health check (GPU, VRAM, Ollama status)
   - `index [path]` — Index a codebase (parse, chunk, embed, store)
   - `search <query>` — Search with natural language
   - `status [path]` — Show index stats
   - `context <query>` — Build token-budgeted context
   - `ask <question>` — Ask about your codebase (local retrieval + Claude reasoning)

6. **How it works** — Brief 3-step explanation: (1) Index: chunks and embeds your code locally via Ollama, stores in LanceDB. (2) Retrieve: searches embeddings for relevant chunks when you ask a question. (3) Reason: sends only the best context to Claude, dramatically reducing tokens.

7. **Tech Stack** — Brief list: TypeScript, Node.js 22, Ollama, LanceDB, Commander, MCP SDK. Keep it casual — "No Postgres. No Redis. No Docker. Just your machine."

8. **Requirements** — Node.js 22+, Ollama running locally, GPU optional (CPU fallback works, just slower).

9. **Status** — "v0.1.0 — MVP shipped. 224 tests passing. Works on the developer's machine (and hopefully yours too)."

10. **License** — MIT (or placeholder if no LICENSE file exists — check first).

Tone guidelines:
- Write like a developer README, not a product page
- Self-deprecating humor is welcome ("yes, another CLI tool")
- Keep it scannable — headers, short paragraphs, code blocks
- No emoji unless it genuinely helps readability (a single one in the tagline area is fine)
- Avoid buzzwords: "revolutionary", "cutting-edge", "seamless", "leverage"
  </action>
  <verify>
    <automated>test -f /workspace/README.md && grep -q "brain-cache" /workspace/README.md && echo "README exists with brain-cache mention" || echo "FAIL"</automated>
  </verify>
  <done>README.md exists at project root with all sections: title, what/why, quick start, CLI reference, how it works, tech stack, requirements, status. Tone is light-hearted and developer-friendly. Uses "brain-cache" (hyphenated) throughout.</done>
</task>

</tasks>

<verification>
- README.md exists at project root
- Contains "brain-cache" (hyphenated, not "braincache")
- Has quick start section with CLI commands
- Has all 7 CLI commands documented
- Tone is casual/light-hearted, not corporate
</verification>

<success_criteria>
- README.md written and committed
- All CLI commands documented
- Light-hearted tone throughout
- Scannable structure with headers and code blocks
</success_criteria>

<output>
After completion, create `.planning/quick/260401-0ka-create-a-light-hearted-readme-md-for-the/260401-0ka-SUMMARY.md`
</output>
