---
phase: 16-retrieval-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/config.ts
  - src/services/retriever.ts
  - tests/services/retriever.test.ts
autonomous: true
requirements: [INTENT-01]

must_haves:
  truths:
    - "A lookup query returns narrow, high-precision results with tight distance threshold"
    - "A trace query triggers seed search with low limit for BFS expansion"
    - "An explore query returns broad results across modules with relaxed threshold"
    - "Multi-word keyword ambiguity is resolved correctly (e.g. 'trace the architecture' = explore, not trace)"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "QueryIntent = 'lookup' | 'trace' | 'explore'"
      contains: "lookup.*trace.*explore"
    - path: "src/services/retriever.ts"
      provides: "classifyRetrievalMode function and 3-mode RETRIEVAL_STRATEGIES"
      exports: ["classifyRetrievalMode", "RETRIEVAL_STRATEGIES"]
    - path: "tests/services/retriever.test.ts"
      provides: "Tests for all three retrieval modes and keyword classification"
  key_links:
    - from: "src/services/retriever.ts"
      to: "src/lib/types.ts"
      via: "import QueryIntent"
      pattern: "import.*QueryIntent.*types"
---

<objective>
Expand the intent classifier from two modes (diagnostic/knowledge) to three modes (lookup/trace/explore), each with distinct retrieval parameters.

Purpose: Enable intent-aware retrieval routing so queries get appropriate search depth and precision.
Output: Updated types, classifier function, strategy map, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-retrieval-intelligence/16-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/types.ts (current - will be modified):
```typescript
export type QueryIntent = 'diagnostic' | 'knowledge';

export interface SearchOptions {
  limit: number;
  distanceThreshold: number;
}
```

From src/services/retriever.ts (current exports - will be modified):
```typescript
export function classifyQueryIntent(query: string): QueryIntent;
export const RETRIEVAL_STRATEGIES: Record<QueryIntent, SearchOptions>;
export async function searchChunks(table: Table, queryVector: number[], opts: SearchOptions): Promise<RetrievedChunk[]>;
export function deduplicateChunks(chunks: RetrievedChunk[]): RetrievedChunk[];
```

From src/lib/config.ts (current constants - some will be removed):
```typescript
export const DEFAULT_SEARCH_LIMIT = 10;
export const DEFAULT_DISTANCE_THRESHOLD = 0.4;
export const DIAGNOSTIC_DISTANCE_THRESHOLD = 0.45;
export const DIAGNOSTIC_SEARCH_LIMIT = 20;
```

Call sites importing classifyQueryIntent/RETRIEVAL_STRATEGIES:
- src/workflows/buildContext.ts (line 10-12)
- src/workflows/search.ts (line 7-11)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update types and implement three-mode classifier with strategy map</name>
  <files>src/lib/types.ts, src/lib/config.ts, src/services/retriever.ts, tests/services/retriever.test.ts</files>
  <read_first>
    - src/lib/types.ts
    - src/lib/config.ts
    - src/services/retriever.ts
    - tests/services/retriever.test.ts
    - src/workflows/buildContext.ts
    - src/workflows/search.ts
  </read_first>
  <behavior>
    - classifyRetrievalMode('find the definition of foo') returns 'lookup'
    - classifyRetrievalMode('where is the auth handler') returns 'lookup'
    - classifyRetrievalMode('signature of classifyQueryIntent') returns 'lookup'
    - classifyRetrievalMode('trace the call path from CLI to LanceDB') returns 'trace'
    - classifyRetrievalMode('how does indexing flow from CLI to storage') returns 'trace'
    - classifyRetrievalMode('trace flow of build_context') returns 'trace'
    - classifyRetrievalMode('explain the architecture') returns 'explore'
    - classifyRetrievalMode('how does auth work') returns 'explore'
    - classifyRetrievalMode('walk me through the retrieval pipeline') returns 'explore'
    - classifyRetrievalMode('trace the architecture of this repo') returns 'explore' (ambiguity: 'trace the' + architecture context = explore wins)
    - RETRIEVAL_STRATEGIES['lookup'] equals { limit: 5, distanceThreshold: 0.25 }
    - RETRIEVAL_STRATEGIES['trace'] equals { limit: 3, distanceThreshold: 0.30 }
    - RETRIEVAL_STRATEGIES['explore'] equals { limit: 20, distanceThreshold: 0.45 }
    - Existing diagnostic bigram tests (stack trace, null pointer, type error, not working) remap to 'lookup' mode
    - Existing exclusion tests (error handler, null object pattern) remap to 'explore' mode
  </behavior>
  <action>
    1. In `src/lib/types.ts`: Change `QueryIntent` from `'diagnostic' | 'knowledge'` to `'lookup' | 'trace' | 'explore'`. Add `FlowHop` interface for Phase 16 Plan 02 (flow tracer):
       ```typescript
       export type QueryIntent = 'lookup' | 'trace' | 'explore';
       
       export interface FlowHop {
         chunkId: string;
         filePath: string;
         name: string | null;
         startLine: number;
         endLine: number;
         content: string;
         hopDepth: number;
       }
       ```

    2. In `src/lib/config.ts`: Remove `DIAGNOSTIC_DISTANCE_THRESHOLD` and `DIAGNOSTIC_SEARCH_LIMIT` constants (they are replaced by inline values in `RETRIEVAL_STRATEGIES`). Keep `DEFAULT_SEARCH_LIMIT` and `DEFAULT_DISTANCE_THRESHOLD` as they may be used elsewhere.

    3. In `src/services/retriever.ts`:
       - Remove imports of `DIAGNOSTIC_SEARCH_LIMIT` and `DIAGNOSTIC_DISTANCE_THRESHOLD` from config.
       - Rename `classifyQueryIntent` to `classifyRetrievalMode`. Keep `classifyQueryIntent` as a deprecated re-export alias: `export const classifyQueryIntent = classifyRetrievalMode;`
       - Replace `DIAGNOSTIC_KEYWORDS`/`DIAGNOSTIC_BIGRAMS`/`DIAGNOSTIC_EXCLUSIONS` with three new keyword arrays:
         - `TRACE_KEYWORDS`: multi-word phrases only — `['trace the', 'trace flow', 'call path', 'flow of', 'follows from', 'calls into', 'invokes', 'trace from', 'how does.*flow']`
         - `LOOKUP_KEYWORDS`: `['where is', 'find the', 'definition of', 'signature of', 'show me the', 'what does.*return', 'what is the type']`
         - Keep existing `DIAGNOSTIC_BIGRAMS` (stack trace, null pointer, etc.) as `LOOKUP_BIGRAMS` — these are high-confidence lookup signals
         - Keep existing `DIAGNOSTIC_EXCLUSIONS` as `EXPLORE_EXCLUSIONS` — when a lookup keyword matches but an exclusion also matches, classify as explore
       - Classification logic in `classifyRetrievalMode`:
         1. Check TRACE_KEYWORDS first (multi-word phrases, regex for 'how does.*flow')
         2. Check LOOKUP_BIGRAMS (strong diagnostic-style signals → lookup)
         3. Check LOOKUP_KEYWORDS with EXPLORE_EXCLUSIONS guard (same pattern as current diagnostic)
         4. Default to 'explore'
       - Update `RETRIEVAL_STRATEGIES` to use new mode names with values from research: lookup={limit:5, distanceThreshold:0.25}, trace={limit:3, distanceThreshold:0.30}, explore={limit:20, distanceThreshold:0.45}

    4. In `src/workflows/buildContext.ts` and `src/workflows/search.ts`: Update import of `classifyQueryIntent` to `classifyRetrievalMode`. Update the variable name from `intent` to `mode` for clarity. The `RETRIEVAL_STRATEGIES[mode]` access works unchanged since the type matches.

    5. In `tests/services/retriever.test.ts`:
       - Update imports: `classifyRetrievalMode` instead of `classifyQueryIntent`
       - Rewrite `classifyQueryIntent` describe block as `classifyRetrievalMode`:
         - All diagnostic → lookup (bigram-based: stack trace, null pointer, type error, not working)
         - All knowledge → explore (architecture, explain, general knowledge)
         - Add trace mode tests (trace the call path, flow of X, how does X flow)
         - Keep exclusion tests: error handler → explore, null object pattern → explore
         - Add ambiguity test: 'trace the architecture' → explore
       - Update `RETRIEVAL_STRATEGIES` tests with new values
  </action>
  <verify>
    <automated>cd /workspace && npx vitest run tests/services/retriever.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'lookup' | 'trace' | 'explore'" src/lib/types.ts || grep -q "lookup.*trace.*explore" src/lib/types.ts
    - grep -q "classifyRetrievalMode" src/services/retriever.ts
    - grep -q "TRACE_KEYWORDS" src/services/retriever.ts
    - grep -q "LOOKUP_KEYWORDS" src/services/retriever.ts
    - grep -q "limit: 5" src/services/retriever.ts
    - grep -q "limit: 3" src/services/retriever.ts
    - grep -q "limit: 20" src/services/retriever.ts
    - grep -q "classifyRetrievalMode" src/workflows/buildContext.ts
    - grep -q "classifyRetrievalMode" src/workflows/search.ts
    - grep -q "FlowHop" src/lib/types.ts
    - npx vitest run tests/services/retriever.test.ts exits 0
  </acceptance_criteria>
  <done>
    QueryIntent type is 'lookup' | 'trace' | 'explore'. classifyRetrievalMode correctly classifies all three modes with multi-word keyword matching. RETRIEVAL_STRATEGIES has mode-specific limits and thresholds. All retriever tests pass. buildContext.ts and search.ts use classifyRetrievalMode. FlowHop interface exported from types.ts.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/services/retriever.test.ts` — all tests pass
- `npx tsc --noEmit` — no TypeScript errors
- `npx vitest run` — full suite passes (no regressions from type/import changes)
</verification>

<success_criteria>
- Three-mode intent classification is working and tested
- Strategy map returns correct parameters for each mode
- No regressions in existing retriever functionality (searchChunks, deduplicateChunks)
- Call sites in buildContext.ts and search.ts compile and use new function name
</success_criteria>

<output>
After completion, create `.planning/phases/16-retrieval-intelligence/16-01-SUMMARY.md`
</output>
