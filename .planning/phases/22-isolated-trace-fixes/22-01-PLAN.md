---
phase: 22-isolated-trace-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/flowTracer.ts
  - tests/services/flowTracer.test.ts
autonomous: true
requirements:
  - OUT-01

must_haves:
  truths:
    - "Each hop in a trace_flow result lists each callee name at most once in its callsFound array"
    - "Duplicate to_symbol edges in the edges table do not produce duplicate callsFound entries"
  artifacts:
    - path: "src/services/flowTracer.ts"
      provides: "Set-based dedup of callsFound"
      contains: "new Set(callEdges.map"
    - path: "tests/services/flowTracer.test.ts"
      provides: "Dedup test case"
      contains: "duplicate"
  key_links:
    - from: "src/services/flowTracer.ts"
      to: "callsFound output"
      via: "Set spread on callEdges.map"
      pattern: "\\[\\.\\.\\.new Set\\(callEdges\\.map"
---

<objective>
Deduplicate callsFound entries in trace_flow hop output (OUT-01).

Purpose: When the edges table contains duplicate rows for the same (from_chunk_id, to_symbol) pair — which happens during incremental index updates — the trace_flow output currently repeats callee names. This fix ensures each callee appears exactly once per hop.

Output: Updated flowTracer.ts with Set-based dedup; new test case covering the duplicate scenario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-isolated-trace-fixes/22-RESEARCH.md
@src/services/flowTracer.ts
@tests/services/flowTracer.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test for callsFound deduplication</name>
  <files>tests/services/flowTracer.test.ts</files>
  <read_first>
    - tests/services/flowTracer.test.ts
    - src/services/flowTracer.ts
  </read_first>
  <behavior>
    - Test: When edges table returns two edges with the same to_symbol value (e.g., two edges both with to_symbol='funcA'), callsFound should contain ['funcA'] not ['funcA', 'funcA']
    - Test: When edges table returns three edges with two unique to_symbol values (funcA, funcA, funcB), callsFound should contain ['funcA', 'funcB']
  </behavior>
  <action>
Add a new test case inside the existing `describe('traceFlow -- callsFound')` block in `tests/services/flowTracer.test.ts`.

Test name: `'deduplicates callsFound when edges table has duplicate to_symbol entries (OUT-01)'`

Setup:
- Create a seed chunk row: `makeChunkRow({ id: 'seed', name: 'seedFunc', file_path: 'src/a.ts' })`
- Mock `mockQueryEdgesFrom` to return 3 edges from seed, two with `to_symbol: 'funcA'` and one with `to_symbol: 'funcB'`:
  ```
  mockQueryEdgesFrom.mockResolvedValueOnce([
    makeEdgeRow({ from_chunk_id: 'seed', to_symbol: 'funcA', from_file: 'src/a.ts' }),
    makeEdgeRow({ from_chunk_id: 'seed', to_symbol: 'funcA', from_file: 'src/a.ts' }),
    makeEdgeRow({ from_chunk_id: 'seed', to_symbol: 'funcB', from_file: 'src/a.ts' }),
  ]);
  ```
- Create chunksTable via `makeSequentialTable` with responses for seed lookup, then two dead-end symbol resolutions (funcA and funcB both resolve to empty):
  ```
  const chunksTable = makeSequentialTable([
    [seedRow],  // lookup seed
    [],         // resolve funcA -> not found
    [],         // resolve funcB -> not found
  ]);
  ```
- Call `const result = await traceFlow({} as Table, chunksTable, 'seed');`
- Assert: `expect(result[0].callsFound).toEqual(['funcA', 'funcB']);`

This test MUST FAIL against the current code (which produces `['funcA', 'funcA', 'funcB']`).
  </action>
  <verify>
    <automated>cd /workspace && npx vitest run tests/services/flowTracer.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/services/flowTracer.test.ts contains a test with string 'deduplicates callsFound when edges table has duplicate to_symbol entries'
    - The test calls `makeEdgeRow` with `to_symbol: 'funcA'` at least twice in the same mockResolvedValueOnce array
    - The test asserts `expect(result[0].callsFound).toEqual(['funcA', 'funcB'])`
    - Running `npx vitest run tests/services/flowTracer.test.ts` shows the new test FAILING (RED phase)
  </acceptance_criteria>
  <done>New test case exists and fails, proving the dedup bug is observable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply Set-based dedup to callsFound in flowTracer.ts</name>
  <files>src/services/flowTracer.ts</files>
  <read_first>
    - src/services/flowTracer.ts
    - tests/services/flowTracer.test.ts
  </read_first>
  <behavior>
    - callsFound with duplicate to_symbol edges produces unique entries only
    - callsFound with all unique to_symbol edges is unchanged
    - Insertion order is preserved (Set maintains insertion order)
  </behavior>
  <action>
In `src/services/flowTracer.ts`, line 102, change:

```typescript
callsFound: callEdges.map(e => e.to_symbol),
```

to:

```typescript
callsFound: [...new Set(callEdges.map(e => e.to_symbol))],
```

This is the ONLY change in this file. Do not modify any other line. The `FlowHop` type (`callsFound: string[]`) remains unchanged. No imports need to be added — `Set` is a built-in.
  </action>
  <verify>
    <automated>cd /workspace && npx vitest run tests/services/flowTracer.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/services/flowTracer.ts line 102 contains `[...new Set(callEdges.map(e => e.to_symbol))]`
    - src/services/flowTracer.ts does NOT contain `callsFound: callEdges.map(e => e.to_symbol),` (the old pattern without Set)
    - Running `npx vitest run tests/services/flowTracer.test.ts` shows ALL tests passing (GREEN phase)
  </acceptance_criteria>
  <done>callsFound is deduplicated via Set; all existing + new tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/services/flowTracer.test.ts` — all tests pass
- `npm test` — full suite green (no regressions)
- grep confirms: `grep -n 'new Set(callEdges' src/services/flowTracer.ts` returns line 102
</verification>

<success_criteria>
- Each hop's callsFound array contains unique callee names only
- No regressions in existing flowTracer tests (cycle detection, maxHops, dead ends)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-isolated-trace-fixes/22-01-SUMMARY.md`
</output>
