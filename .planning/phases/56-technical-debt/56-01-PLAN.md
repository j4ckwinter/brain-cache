---
phase: 56-technical-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/retriever.ts
  - src/services/index.ts
  - tests/workflows/buildContext.test.ts
  - tests/workflows/search.test.ts
  - src/tools/index.ts
autonomous: true
requirements: [DEBT-02, DEBT-03]

must_haves:
  truths:
    - "classifyQueryIntent is not importable from the services barrel"
    - "No file in src/ or tests/ references classifyQueryIntent"
    - "src/tools/ directory does not exist in the source tree"
  artifacts:
    - path: "src/services/retriever.ts"
      provides: "classifyRetrievalMode without deprecated alias"
      contains: "classifyRetrievalMode"
    - path: "src/services/index.ts"
      provides: "Clean barrel without classifyQueryIntent re-export"
  key_links:
    - from: "src/services/index.ts"
      to: "src/services/retriever.ts"
      via: "re-export"
      pattern: "classifyRetrievalMode"
---

<objective>
Remove deprecated classifyQueryIntent export and delete the empty src/tools/ directory.

Purpose: Eliminate dead code surface area (DEBT-02) and remove a placeholder directory that creates false expectations (DEBT-03).
Output: Cleaner services barrel, no src/tools/ directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-technical-debt/56-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove classifyQueryIntent deprecated alias and update tests (DEBT-02)</name>
  <files>src/services/retriever.ts, src/services/index.ts, tests/workflows/buildContext.test.ts, tests/workflows/search.test.ts</files>
  <read_first>src/services/retriever.ts, src/services/index.ts, tests/workflows/buildContext.test.ts, tests/workflows/search.test.ts</read_first>
  <action>
1. In `src/services/retriever.ts`, delete line 99: `export const classifyQueryIntent = classifyRetrievalMode;` and the `@deprecated` JSDoc comment above it (line 98).

2. In `src/services/index.ts`, in the re-export block from `./retriever.js` (lines 57-62), remove `classifyQueryIntent,` from the named exports. The block should become:
```typescript
export {
  searchChunks,
  deduplicateChunks,
  RETRIEVAL_STRATEGIES,
} from './retriever.js';
```

3. In `tests/workflows/buildContext.test.ts`, find the mock shape around line 26 that includes `classifyQueryIntent: vi.fn()` and remove that line. Do NOT remove other mock properties.

4. In `tests/workflows/search.test.ts`, find the test description at line 146 that says "calls classifyQueryIntent with the query" and change it to "calls classifyRetrievalMode with the query".

5. Run `grep -rn classifyQueryIntent src/ tests/` to confirm zero results. The only acceptable remaining reference is if `tests/services/retriever.test.ts` uses the string "classifyQueryIntent" as example query text content (not an import or call) — that is a query content string and does NOT need changing.
  </action>
  <verify>
    <automated>grep -rn "classifyQueryIntent" src/ tests/ | grep -v "query text" | wc -l | grep -q "^0$" && npx vitest run tests/workflows/buildContext.test.ts tests/workflows/search.test.ts tests/services/retriever.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "classifyQueryIntent" src/services/retriever.ts` returns zero lines
    - `grep -rn "classifyQueryIntent" src/services/index.ts` returns zero lines
    - `grep -rn "classifyQueryIntent" tests/workflows/buildContext.test.ts` returns zero lines
    - `tests/workflows/search.test.ts` contains "calls classifyRetrievalMode with the query" instead of "calls classifyQueryIntent with the query"
    - `npx vitest run tests/workflows/buildContext.test.ts tests/workflows/search.test.ts tests/services/retriever.test.ts` exits 0
  </acceptance_criteria>
  <done>classifyQueryIntent alias is completely removed from production code, barrel, and test mocks. All affected tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete empty src/tools/ directory (DEBT-03)</name>
  <files>src/tools/index.ts</files>
  <read_first>src/tools/index.ts</read_first>
  <action>
1. Delete `src/tools/index.ts` and the `src/tools/` directory:
```bash
rm -rf src/tools/
```

2. Verify no imports reference src/tools/:
```bash
grep -rn "from.*['\"].*tools" src/ | grep -v node_modules | grep -v ".test."
```
This should return zero results referencing `src/tools/`.

3. Verify tsup.config.ts entry points do NOT include src/tools/ (they should be `src/cli/index.ts` and `src/mcp/main.ts` only).

4. Run `npx tsc --noEmit` to confirm no broken imports.
  </action>
  <verify>
    <automated>test ! -d src/tools && echo "PASS: src/tools/ deleted" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/tools/ 2>&1` outputs "No such file or directory"
    - `npx tsc --noEmit` exits 0
    - `grep -rn "from.*tools" src/ | grep -v node_modules | grep -v "from.*tools/"` shows no imports from deleted module
  </acceptance_criteria>
  <done>src/tools/ directory is removed from the source tree. TypeScript compilation succeeds. No broken imports.</done>
</task>

</tasks>

<verification>
- `grep -rn classifyQueryIntent src/ tests/` returns at most the query-text-content reference in retriever.test.ts
- `test ! -d src/tools` succeeds
- `npx vitest run --reporter=verbose` full suite passes
</verification>

<success_criteria>
- classifyQueryIntent is not importable from any barrel or module
- src/tools/ directory does not exist
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/56-technical-debt/56-01-SUMMARY.md`
</output>
