---
phase: 33-reset-to-v1-core
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - src/mcp/index.ts
  - src/services/index.ts
  - src/workflows/index.ts
  - src/workflows/buildContext.ts
  - src/lib/types.ts
  - tests/mcp/server.test.ts
  - tests/services/lancedb.test.ts
  - tests/services/chunker.test.ts
  - tests/services/retriever.test.ts
  - tests/workflows/index.test.ts
  - tests/workflows/buildContext.test.ts
autonomous: true
requirements:
  - SKILL-02
must_haves:
  truths:
    - "npm run build succeeds with zero errors"
    - "All surviving tests pass"
    - "MCP server registers exactly 3 tools plus doctor (no trace_flow, no explain_codebase)"
  artifacts:
    - path: "src/mcp/index.ts"
      provides: "MCP server with 3 tools + doctor"
      contains: "index_repo"
    - path: "dist/"
      provides: "Compiled output from successful build"
  key_links:
    - from: "src/mcp/index.ts"
      to: "src/workflows/buildContext.ts"
      via: "runBuildContext import"
      pattern: "import.*runBuildContext.*from.*buildContext"
    - from: "src/mcp/index.ts"
      to: "src/workflows/search.ts"
      via: "runSearch import"
      pattern: "import.*runSearch.*from.*search"
    - from: "src/mcp/index.ts"
      to: "src/workflows/index.ts"
      via: "runIndex import"
      pattern: "import.*runIndex.*from.*index"
---

<objective>
Verify the reset branch compiles, all surviving tests pass, and the MCP server registers exactly the correct tool set.

Purpose: Ensure the ported codebase is a working, shippable state — not just a collection of files.

Output: Green build, passing tests, validated 3-tool MCP server.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-reset-to-v1-core/33-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix compilation errors and ensure build succeeds</name>
  <read_first>
    - src/mcp/index.ts (verify no imports of removed modules)
    - src/services/index.ts (verify barrel exports are clean)
    - src/workflows/index.ts (verify no imports of removed modules)
    - src/workflows/buildContext.ts (verify no imports of compression/cohesion)
    - src/lib/types.ts (verify type definitions are complete)
    - src/lib/config.ts (verify constants exist)
    - tsconfig.json
    - tsup.config.ts
  </read_first>
  <files>
    src/mcp/index.ts
    src/services/index.ts
    src/workflows/index.ts
    src/workflows/buildContext.ts
    src/lib/types.ts
    src/lib/config.ts
    tsconfig.json
    tsup.config.ts
  </files>
  <action>
    **Step 1: Install dependencies**
    ```
    npm install
    ```

    **Step 2: Run TypeScript compilation check**
    ```
    npx tsc --noEmit
    ```

    **Step 3: Fix any compilation errors**
    Common issues to expect and fix:
    - Missing type imports (CallEdge, ChunkResult) — add to src/lib/types.ts if not present
    - Missing function imports in barrel (src/services/index.ts) — add exports for new hardening functions
    - Stale imports in MCP server referencing format.ts or removed workflows — replace with inline string formatting
    - Missing edge table functions (openOrCreateEdgesTable, insertEdges, queryEdgesFrom) in lancedb.ts — these are needed by incremental indexing for call edge storage
    - The index workflow (src/workflows/index.ts) may reference `loadIgnorePatterns` — remove this import and the .braincacheignore loading code block, since ignorePatterns.ts is cut
    - The index workflow may reference `formatTokenSavings` from format.ts — replace with simple template string formatting
    - buildContext may import from compression.ts or use cohesion grouping — remove those imports and simplify to v1.0-level context assembly
    - The crawler's `extraIgnorePatterns` option can stay in the function signature (it's part of crawler.ts) but the `loadIgnorePatterns` import in index.ts must be removed

    **Step 4: Run the build**
    ```
    npm run build
    ```

    **Step 5: Verify dist/ output**
    ```
    ls dist/
    ```
    Should produce compiled output without errors.

    **Key principle:** When fixing compilation errors, prefer REMOVING v2.0+ code over adding compatibility shims. The goal is simplification. If a function depends on a removed service, rewrite it to work without that service rather than stubbing the dependency.
  </action>
  <verify>
    <automated>cd /workspace && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` exits with code 0
    - `ls dist/` shows compiled output files
    - `grep -rn "flowTracer\|cohesion\|compression\|fileWatcher\|configLoader\|ignorePatterns\|sessionStats\|traceFlow\|explainCodebase\|watch\|claude-md-section\|format\.\|statusline" src/ --include="*.ts"` returns no matches (no stale imports)
  </acceptance_criteria>
  <done>TypeScript compilation succeeds. Build produces dist/ output. No references to removed v2.0+ modules remain in source.</done>
</task>

<task type="auto">
  <name>Task 2: Fix tests and validate MCP tool registration</name>
  <read_first>
    - tests/mcp/server.test.ts
    - tests/services/lancedb.test.ts
    - tests/services/chunker.test.ts
    - tests/services/retriever.test.ts
    - tests/workflows/index.test.ts
    - tests/workflows/buildContext.test.ts
  </read_first>
  <files>
    tests/mcp/server.test.ts
    tests/services/lancedb.test.ts
    tests/services/chunker.test.ts
    tests/services/retriever.test.ts
    tests/workflows/index.test.ts
    tests/workflows/buildContext.test.ts
    tests/workflows/search.test.ts
  </files>
  <action>
    **Step 1: Remove test files for removed services**
    Delete any test files that test v2.0+ services (they should already be absent on the v1.0-based branch, but verify):
    ```
    rm -f tests/services/cohesion.test.ts
    rm -f tests/services/compression.test.ts
    rm -f tests/services/configLoader.test.ts
    rm -f tests/services/fileWatcher.test.ts
    rm -f tests/services/flowTracer.test.ts
    rm -f tests/services/ignorePatterns.test.ts
    rm -f tests/services/sessionStats.test.ts
    rm -f tests/workflows/traceFlow.test.ts
    rm -f tests/workflows/explainCodebase.test.ts
    rm -f tests/workflows/watch.test.ts
    rm -f tests/lib/claude-md-section.test.ts
    rm -f tests/lib/format.test.ts
    rm -f tests/scripts/statusline.test.ts
    rm -f tests/scripts/statusline.integration.test.ts
    ```

    **Step 2: Run all tests**
    ```
    npm test
    ```

    **Step 3: Fix failing tests**
    Common issues to expect:
    - Tests importing from removed modules — update imports
    - MCP server test may check for 6 tools — update to check for 4 (index_repo, search_codebase, build_context, doctor)
    - Chunker tests may not expect the new `{ chunks, edges }` return type — update expectations
    - LanceDB tests may not include hash manifest function tests — these were added in Phase 10, the test should already exist on v1.0 if we ported correctly. If not, add basic tests for readFileHashes/writeFileHashes
    - Retriever tests may reference classifyQueryIntent with 'trace' mode — remove trace-mode test cases
    - Index workflow tests may reference .braincacheignore — remove those test cases
    - buildContext tests may reference compression or cohesion — simplify to v1.0-level expectations

    **Step 4: Validate MCP tool count**
    The MCP server test (tests/mcp/server.test.ts) should verify exactly these tools are registered:
    1. `index_repo`
    2. `search_codebase`
    3. `build_context`
    4. `doctor`

    If the test checks tool count, update to expect 4. If it checks tool names, update the expected list.

    Add or update a test assertion:
    ```typescript
    // In the MCP server test
    expect(registeredTools).toHaveLength(4);
    expect(registeredTools.map(t => t.name).sort()).toEqual([
      'build_context', 'doctor', 'index_repo', 'search_codebase'
    ]);
    ```

    **Step 5: Final test run**
    ```
    npm test
    ```
    All tests must pass.
  </action>
  <verify>
    <automated>cd /workspace && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0
    - `npm test 2>&1 | grep -c "fail"` returns 0 (case-insensitive check for test failures)
    - `grep -c "registerTool" src/mcp/index.ts` returns exactly 4 (index_repo, search_codebase, build_context, doctor)
    - No test files exist for removed services: `ls tests/services/flowTracer.test.ts tests/services/cohesion.test.ts tests/services/compression.test.ts tests/workflows/traceFlow.test.ts tests/workflows/explainCodebase.test.ts 2>&1 | grep -c "No such file"` returns 5
  </acceptance_criteria>
  <done>All surviving tests pass. MCP server registers exactly 4 tools (index_repo, search_codebase, build_context, doctor). No test files for removed services exist.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npm test` passes all tests
- `grep -c "registerTool" src/mcp/index.ts` returns 4
- `git diff --stat v1.0..HEAD -- src/` shows only additions/modifications to v1.0 files, no new v2.0+ files
- Source tree contains ~23 src files (v1.0 core)
</verification>

<success_criteria>
- Build succeeds with zero TypeScript errors
- All tests pass
- MCP server exposes exactly 3 user-facing tools (index_repo, search_codebase, build_context) plus doctor diagnostic tool
- Branch is in a clean, shippable state ready for Phase 34 (status line cherry-pick)
</success_criteria>

<output>
After completion, create `.planning/phases/33-reset-to-v1-core/33-02-SUMMARY.md`
</output>
