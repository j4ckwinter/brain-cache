---
phase: 33-reset-to-v1-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/capability.ts
  - src/services/chunker.ts
  - src/services/crawler.ts
  - src/services/embedder.ts
  - src/services/lancedb.ts
  - src/services/logger.ts
  - src/services/ollama.ts
  - src/services/retriever.ts
  - src/services/tokenCounter.ts
  - src/services/index.ts
  - src/workflows/index.ts
  - src/workflows/buildContext.ts
  - src/workflows/search.ts
  - src/lib/config.ts
  - src/lib/types.ts
  - package.json
autonomous: true
requirements:
  - SKILL-01
must_haves:
  truths:
    - "Branch v3.0-skill-reshape exists based on v1.0 tag"
    - "Source tree contains only v1.0 services plus incremental indexing additions"
    - "No v2.0+ files exist (flowTracer, cohesion, compression, fileWatcher, explainCodebase, configLoader, traceFlow, watch, ignorePatterns, claude-md-section, format, statusline-script, sessionStats)"
    - "Hardening fixes from phases 6-12 are applied to core services"
    - "Package.json has current dependency versions without unused deps (no chokidar, no dedent)"
  artifacts:
    - path: "src/services/lancedb.ts"
      provides: "LanceDB service with hash manifest functions and deleteChunksByFilePath"
      contains: "readFileHashes"
    - path: "src/services/ollama.ts"
      provides: "Ollama service with pre-spawn guard and OLLAMA_HOST remote check"
      contains: "OLLAMA_HOST"
    - path: "src/workflows/index.ts"
      provides: "Index workflow with incremental indexing (SHA-256 hash diff)"
      contains: "hashContent"
  key_links:
    - from: "src/workflows/index.ts"
      to: "src/services/lancedb.ts"
      via: "readFileHashes, writeFileHashes, deleteChunksByFilePath"
      pattern: "readFileHashes|writeFileHashes|deleteChunksByFilePath"
---

<objective>
Create a fresh branch from the v1.0 git tag and port forward the hardening fixes (phases 6-12) and incremental indexing (phase 10) onto the v1.0 core services.

Purpose: Reset the codebase to its essential ~23 files, shedding all v2.0+ feature bloat while keeping the battle-tested hardening improvements.

Output: Branch `v3.0-skill-reshape` with clean v1.0 source tree plus hardening and incremental indexing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branch from v1.0 and port hardening + incremental indexing</name>
  <read_first>
    - Run `git show v1.0:src/services/ollama.ts` to see v1.0 baseline
    - Run `git show v1.0:src/services/lancedb.ts` to see v1.0 baseline
    - Run `git show v1.0:src/workflows/index.ts` to see v1.0 baseline
    - Run `git show v1.0:src/lib/config.ts` to see v1.0 baseline
    - Run `git show v1.0:src/lib/types.ts` to see v1.0 baseline
    - Run `git show v1.0:src/services/chunker.ts` to see v1.0 baseline
    - Run `git show v1.0:package.json` to see v1.0 deps
    - Read current HEAD versions of same files to identify what hardening changes need porting
  </read_first>
  <files>
    src/services/capability.ts
    src/services/chunker.ts
    src/services/crawler.ts
    src/services/embedder.ts
    src/services/lancedb.ts
    src/services/logger.ts
    src/services/ollama.ts
    src/services/retriever.ts
    src/services/tokenCounter.ts
    src/services/index.ts
    src/workflows/index.ts
    src/workflows/buildContext.ts
    src/workflows/search.ts
    src/workflows/init.ts
    src/workflows/doctor.ts
    src/workflows/status.ts
    src/workflows/askCodebase.ts
    src/lib/config.ts
    src/lib/types.ts
    src/lib/index.ts
    src/mcp/index.ts
    src/cli/index.ts
    src/tools/index.ts
    package.json
    tsconfig.json
    tsup.config.ts
    vitest.config.ts
  </files>
  <action>
    **Step 1: Create branch from v1.0 tag**
    ```
    git checkout -b v3.0-skill-reshape v1.0
    ```

    **Step 2: Port hardening fixes to core services**

    The v1.0 codebase needs these specific hardening changes applied manually (do NOT cherry-pick commits — they touch v2.0 files that don't exist on this branch). Instead, read the current HEAD version of each file and manually apply only the hardening improvements to the v1.0 baseline.

    Changes to port per service (compare v1.0 vs current HEAD, apply only hardening — NOT v2.0 features):

    **src/services/ollama.ts** (Phase 8 + Phase 12):
    - Add pre-spawn PID guard to `startOllama()` to prevent spawning duplicate Ollama processes
    - Add `OLLAMA_HOST` environment variable guard — if OLLAMA_HOST is set and points to a remote host, skip spawning and just verify connectivity
    - Add `getOllamaHost()` export

    **src/services/lancedb.ts** (Phase 9 + Phase 10):
    - Add `createVectorIndexIfNeeded()` function for large tables
    - Add `readFileHashes()` and `writeFileHashes()` for SHA-256 hash manifest (incremental indexing)
    - Add `deleteChunksByFilePath()` for removing stale chunks
    - Fix ChunkRow index signature (Phase 7)
    - Add `withWriteLock()` mutex wrapper for concurrent writes

    **src/services/chunker.ts** (Phase 10):
    - Add call edge extraction (`CallEdge` type, `ChunkResult` return type with `{ chunks, edges }`)
    - Update `chunkFile()` to return `ChunkResult` instead of just `CodeChunk[]`

    **src/lib/types.ts** (Phase 10 + Phase 15):
    - Add `CallEdge` interface: `{ from: string, to: string, fromFile: string, kind: 'call' | 'import' }`
    - Add `ChunkResult` interface: `{ chunks: CodeChunk[], edges: CallEdge[] }`
    - Ensure `ContextResult` type has metadata with tokensSent, estimatedWithoutBraincache, reductionPct, filesInContext, localTasksPerformed

    **src/lib/config.ts** (Phases 9-12):
    - Add `FILE_READ_CONCURRENCY` constant (default 20)
    - Add `EMBED_MAX_TOKENS` constant (default 1400)
    - Add `DEFAULT_BATCH_SIZE` if not already present
    - Ensure `EMBEDDING_DIMENSIONS` and `DEFAULT_EMBEDDING_DIMENSION` exist

    **src/services/embedder.ts** (Phase 6):
    - Ensure error propagation is correct (errors from Ollama are thrown, not swallowed)
    - Return `{ embeddings, skipped }` tuple for reporting skipped oversized chunks

    **src/services/crawler.ts** (Phase 9):
    - Ensure concurrent file I/O support (already uses fast-glob)
    - Do NOT add `extraIgnorePatterns` or `loadIgnorePatterns` — that was v2.0 (ignorePatterns.ts is being cut)

    **src/services/retriever.ts** (Phase 7 + Phase 10):
    - Type safety fixes from Phase 7
    - Add `classifyQueryIntent()` basic intent classification (lookup vs explore modes — NOT trace mode which was v2.0)
    - Add `RETRIEVAL_STRATEGIES` config
    - Note: Do NOT port keyword boost scoring, noise penalty, or v2.2+ retrieval enhancements — those depend on v2.0 services

    **src/services/tokenCounter.ts** (Phase 9):
    - Add `assembleContext()` with hoisted separator token count optimization
    - Ensure `formatChunk()` and `countChunkTokens()` exports

    **src/workflows/index.ts** (Phase 10 + Phase 11):
    - Replace simple index with incremental indexing workflow:
      - Read all files, compute SHA-256 hashes
      - Diff against stored manifest (readFileHashes)
      - Delete stale chunks (deleteChunksByFilePath)
      - Only re-chunk and re-embed new/changed files
      - Write updated hash manifest
    - Add `force` option to bypass incremental and do full reindex
    - Add concurrent pipeline from Phase 11 (group-based chunk + embed)
    - Do NOT import from ignorePatterns.ts — remove any .braincacheignore references
    - Do NOT import from format.ts — use simple string formatting for stderr output
    - Edge table operations: openOrCreateEdgesTable, insertEdges for call edges from chunker

    **src/mcp/index.ts** (keep only 3 tools + doctor):
    - Remove imports of `runTraceFlow`, `runExplainCodebase`, `accumulateStats`, and all `format.ts` imports
    - Remove Tool 5 (trace_flow) and Tool 6 (explain_codebase) registrations entirely
    - Remove `accumulateStats` calls from search_codebase and build_context handlers
    - Use simple string formatting instead of format.ts helpers (formatToolResponse, formatTokenSavings, etc.)
    - Keep: index_repo, search_codebase, build_context, doctor (4 tool registrations)
    - Add build-time version injection (`__BRAIN_CACHE_VERSION__`)

    **src/services/index.ts** (barrel):
    - Remove exports for: flowTracer, ignorePatterns, sessionStats, cohesion, compression, configLoader, fileWatcher
    - Keep exports for: capability, chunker, crawler, embedder, lancedb, logger, ollama, retriever, tokenCounter
    - Add new exports from hardening: createVectorIndexIfNeeded, readFileHashes, writeFileHashes, deleteChunksByFilePath, withWriteLock, ChunkRow, classifyQueryIntent, RETRIEVAL_STRATEGIES

    **src/workflows/buildContext.ts**:
    - Ensure it does NOT import from compression.ts or cohesion.ts
    - Use the v1.0 version as base, adding only the ContextResult metadata improvements

    **package.json**:
    - Update dependency versions to match current HEAD versions (these are the latest confirmed-working versions)
    - Remove `chokidar` (fileWatcher dependency)
    - Remove `dedent` and `@types/dedent` (format.ts dependency)
    - Keep all other deps unchanged from current HEAD

    **Step 3: Remove any v2.0+ files that might have been accidentally included**
    Verify these files do NOT exist on the branch:
    - src/services/flowTracer.ts
    - src/services/cohesion.ts
    - src/services/compression.ts
    - src/services/fileWatcher.ts
    - src/services/configLoader.ts
    - src/services/ignorePatterns.ts
    - src/services/sessionStats.ts
    - src/workflows/traceFlow.ts
    - src/workflows/explainCodebase.ts
    - src/workflows/watch.ts
    - src/lib/claude-md-section.ts
    - src/lib/format.ts
    - src/lib/statusline-script.ts
    - src/scripts/statusline.mjs
    - src/types/globals.d.ts

    **Step 4: Verify no imports reference removed files**
    ```
    grep -r "flowTracer\|cohesion\|compression\|fileWatcher\|configLoader\|ignorePatterns\|sessionStats\|traceFlow\|explainCodebase\|watch\|claude-md-section\|format\.\|statusline" src/ --include="*.ts" --include="*.mjs"
    ```
    Fix any remaining references.
  </action>
  <verify>
    <automated>cd /workspace && git branch --list v3.0-skill-reshape | grep -q v3.0-skill-reshape && echo "Branch exists" && ls src/services/flowTracer.ts 2>/dev/null && echo "FAIL: flowTracer exists" || echo "OK: no flowTracer" && ls src/services/cohesion.ts 2>/dev/null && echo "FAIL: cohesion exists" || echo "OK: no cohesion" && ls src/services/compression.ts 2>/dev/null && echo "FAIL: compression exists" || echo "OK: no compression" && ls src/workflows/traceFlow.ts 2>/dev/null && echo "FAIL: traceFlow exists" || echo "OK: no traceFlow" && ls src/workflows/explainCodebase.ts 2>/dev/null && echo "FAIL: explainCodebase exists" || echo "OK: no explainCodebase" && grep -c "readFileHashes" src/services/lancedb.ts && grep -c "OLLAMA_HOST" src/services/ollama.ts && grep -c "hashContent\|incremental" src/workflows/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --list v3.0-skill-reshape` returns the branch name
    - `ls src/services/flowTracer.ts` returns "No such file"
    - `ls src/services/cohesion.ts` returns "No such file"
    - `ls src/services/compression.ts` returns "No such file"
    - `ls src/services/fileWatcher.ts` returns "No such file"
    - `ls src/services/configLoader.ts` returns "No such file"
    - `ls src/services/ignorePatterns.ts` returns "No such file"
    - `ls src/services/sessionStats.ts` returns "No such file"
    - `ls src/workflows/traceFlow.ts` returns "No such file"
    - `ls src/workflows/explainCodebase.ts` returns "No such file"
    - `ls src/workflows/watch.ts` returns "No such file"
    - `ls src/lib/claude-md-section.ts` returns "No such file"
    - `ls src/lib/format.ts` returns "No such file"
    - `ls src/lib/statusline-script.ts` returns "No such file"
    - `grep -c "readFileHashes" src/services/lancedb.ts` returns >= 1
    - `grep -c "OLLAMA_HOST" src/services/ollama.ts` returns >= 1
    - `grep -c "hashContent" src/workflows/index.ts` returns >= 1
    - `grep -rn "flowTracer\|cohesion\|compression\|fileWatcher\|configLoader\|ignorePatterns\|sessionStats\|traceFlow\|explainCodebase\|watch\|claude-md-section\|statusline" src/ --include="*.ts"` returns no matches
    - `grep -c "chokidar" package.json` returns 0
    - `grep -c "dedent" package.json` returns 0
  </acceptance_criteria>
  <done>Branch v3.0-skill-reshape exists with only v1.0 core files plus hardening and incremental indexing changes applied. No v2.0+ feature files exist. Package.json has current dep versions without unused deps.</done>
</task>

</tasks>

<verification>
- Branch `v3.0-skill-reshape` exists and is checked out
- Source tree matches v1.0 structure (23 src files) plus hardening additions
- No v2.0+ feature files exist anywhere in src/
- No imports reference removed services
- Package.json contains no unused dependencies
</verification>

<success_criteria>
- Branch created from v1.0 tag with hardening + incremental indexing ported
- Zero references to removed v2.0+ services in any source file
- Package.json cleaned of unused dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/33-reset-to-v1-core/33-01-SUMMARY.md`
</output>
