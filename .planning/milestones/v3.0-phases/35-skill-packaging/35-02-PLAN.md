---
phase: 35-skill-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - SKILL-06

must_haves:
  truths:
    - "README focuses on local embeddings to save money pitch"
    - "README shows exactly 3 MCP tools, not 4 or 6"
    - "README includes skill installation instructions"
    - "CLAUDE.md tool routing table shows only 3 tools"
    - "No references to trace_flow or explain_codebase in either file"
  artifacts:
    - path: "README.md"
      provides: "Focused product pitch and install guide"
      contains: "save money"
    - path: "CLAUDE.md"
      provides: "Simplified 3-tool routing for Claude"
      contains: "search_codebase"
  key_links:
    - from: "README.md"
      to: ".claude/skills/brain-cache/SKILL.md"
      via: "skill installation instructions"
      pattern: "\\.claude/skills"
    - from: "CLAUDE.md"
      to: "MCP server"
      via: "tool routing table"
      pattern: "search_codebase.*build_context.*index_repo"
---

<objective>
Rewrite README.md to match v1.0's focused pitch and simplify CLAUDE.md to the 3-tool surface area.

Purpose: Documentation must match the stripped-down product — "local embeddings to save money" not "6-tool codebase intelligence suite."
Output: Updated README.md and CLAUDE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with focused pitch</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — preserve structure elements that work)
  </read_first>
  <action>
Rewrite README.md with the following structure. Use the v1.0 copy style — punchy, developer-focused, no corporate fluff.

**Header and tagline:**
```
# brain-cache

> Your local GPU finally has a job.

brain-cache is a local AI runtime that sits between your codebase and Claude. It runs embeddings and retrieval on your machine — so Claude only sees what actually matters. Fewer tokens. Better answers. Your API bill stops looking like a mortgage payment.
```

**How it works** (numbered list, same as v1.0):
1. Embeds your query locally via Ollama (fast, free, no API calls)
2. Retrieves the most relevant code chunks from its local vector index
3. Trims and deduplicates the context to fit a tight token budget
4. Hands Claude a clean, minimal context — not your entire repo

**MCP Tools section** — list exactly 3 tools:
- `build_context` — Assembles relevant context for any question. Use instead of reading files.
- `search_codebase` — Finds functions, types, and symbols by meaning, not keyword. Use instead of grep.
- `index_repo` — Rebuilds the local vector index.

Also mention `doctor` as a diagnostic tool (it is part of the MCP server but not a core retrieval tool).

**Example section** — keep the existing token savings example (97% reduction).

**Quick start** — same 3 steps:
1. `npm install -g brain-cache`
2. `brain-cache init` / `brain-cache index`
3. Use Claude normally

**Skill installation section (NEW):**
```
## Install as Claude Code skill

brain-cache ships as a Claude Code skill. After `brain-cache init`, the skill is
installed at `.claude/skills/brain-cache/SKILL.md` in your project. Claude
automatically learns when and how to use brain-cache tools.

To install manually, copy the `.claude/skills/brain-cache/` directory into your
project root.
```

**Status line section:**
Mention that brain-cache shows token savings in Claude Code's status bar after init.

**Token savings footer** section — keep existing content about optional CLAUDE.md footer.

**Requirements:**
- Node.js 22+
- Ollama running locally (nomic-embed-text model)
- Anthropic API key (for `ask` command only)

**CLI commands** — keep existing list but verify no removed commands are listed.

**REMOVE from current README:**
- The "Status" section mentioning "planned" features (reranking, compression, live indexing) — these are cut
- Any reference to 6 tools, trace_flow, explain_codebase
- The "Built with GSD" section
- The "Why it's different" section (replace with the "how it works" v1.0 copy which is stronger)

Do NOT use emojis in section headers. Use plain text headers (## How it works, ## Quick start, etc.).
  </action>
  <verify>
    <automated>grep -c "trace_flow\|explain_codebase\|file.*watch\|cohesion\|compression" README.md | grep -q "^0$" && grep -q "save money\|mortgage\|GPU.*job" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README uses v1.0 punchy copy style ("GPU finally has a job", "mortgage payment")
    - Lists exactly 3 MCP tools (build_context, search_codebase, index_repo) plus doctor
    - Includes skill installation section referencing .claude/skills/brain-cache/
    - No references to trace_flow, explain_codebase, file watching, compression, cohesion
    - No "planned" features section with cut features
    - Requirements section preserved
    - Quick start preserved
    - No emojis in section headers
  </acceptance_criteria>
  <done>README.md reflects focused "local embeddings to save money" pitch with 3-tool surface area and skill install instructions</done>
</task>

<task type="auto">
  <name>Task 2: Simplify CLAUDE.md to 3-tool routing</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current content — identify all sections to modify)
  </read_first>
  <action>
Edit CLAUDE.md to remove all v2.0+ tool references and simplify to the 3-tool surface area. This is surgical — preserve everything unrelated to tool routing.

**Changes to make:**

1. **"Brain-Cache MCP Tools" section** — Replace the tool routing table with:

| Query type | Tool | NOT this |
|-----------|------|---------|
| Locate a function, type, or symbol | `search_codebase` | `build_context` |
| Understand how specific code works across files | `build_context` | file reads |
| Diagnose brain-cache failures | `doctor` | -- |
| Reindex the project | `index_repo` | -- |

2. **Remove these per-tool subsections entirely:**
   - `### trace_flow (trace call paths)` — entire block
   - `### explain_codebase (architecture overview)` — entire block
   - Any disambiguation between build_context and trace_flow

3. **Keep these per-tool subsections** (but verify no references to removed tools):
   - `### search_codebase (locate code)`
   - `### build_context (understand specific behavior)` — remove the line "Do NOT use for architecture overviews (use explain_codebase)" and "Do NOT use for call-path tracing (use trace_flow)" and the "Use trace_flow instead of..." line
   - `### doctor (diagnose issues)`
   - `### index_repo (reindex)`

4. **Remove the duplicate "Brain-Cache MCP Tools" section** — CLAUDE.md currently has the tool table and subsections duplicated twice. Remove the second copy entirely.

5. **Update PROJECT.md description reference in CLAUDE.md** if it mentions 6 tools or v2.0+ features. The Project section at top should say "3 MCP tools" not reference trace_flow or explain_codebase.

6. **Preserve completely (do not touch):**
   - Technology Stack section
   - Conventions section
   - Architecture section
   - GSD Workflow Enforcement section
   - Developer Profile section (even though it references trace_flow — that section is auto-managed by generate-claude-profile)

Actually — the Developer Profile section also has the duplicate tool table and subsections. Clean those up too: remove trace_flow and explain_codebase entries from that table and remove their subsection blocks.

Do NOT change anything in the Technology Stack tables, Constraints, or GSD sections.
  </action>
  <verify>
    <automated>grep -c "trace_flow\|explain_codebase" CLAUDE.md | xargs -I {} sh -c 'if [ {} -eq 0 ]; then echo "PASS"; else echo "FAIL: {} references to removed tools remain"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Tool routing table shows 3 tools (search_codebase, build_context, index_repo) plus doctor
    - No trace_flow subsection or references anywhere
    - No explain_codebase subsection or references anywhere
    - No duplicate tool routing sections (was duplicated, now single)
    - build_context subsection no longer mentions trace_flow or explain_codebase alternatives
    - Technology Stack, Conventions, Architecture, GSD sections untouched
    - File is valid markdown
  </acceptance_criteria>
  <done>CLAUDE.md has single 3-tool routing table, no removed tool references, all non-tool sections preserved</done>
</task>

</tasks>

<verification>
- `grep -c "trace_flow\|explain_codebase" README.md CLAUDE.md` returns 0 for both files
- `grep "search_codebase" CLAUDE.md` returns matches (tool is documented)
- `grep "build_context" CLAUDE.md` returns matches (tool is documented)
- `grep "index_repo" CLAUDE.md` returns matches (tool is documented)
- `grep "skill" README.md` returns matches (skill install documented)
- README.md contains "mortgage" or "GPU" (v1.0 copy style)
</verification>

<success_criteria>
Both README.md and CLAUDE.md reflect the simplified 3-tool brain-cache with focused "save money" messaging. No traces of removed features remain in documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/35-skill-packaging/35-02-SUMMARY.md`
</output>
