---
phase: 34-cherry-pick-status-line
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/sessionStats.ts
  - src/scripts/statusline.mjs
  - src/lib/statusline-script.ts
  - src/lib/config.ts
  - tests/services/sessionStats.test.ts
  - tests/scripts/statusline.test.ts
  - tests/scripts/statusline.integration.test.ts
autonomous: true
requirements:
  - SKILL-03

must_haves:
  truths:
    - "sessionStats.ts accumulates tokensSent and estimatedWithoutBraincache with mutex-serialized atomic writes"
    - "statusline.mjs reads session-stats.json and renders savings or idle output"
    - "All ported tests pass without modification to test logic"
  artifacts:
    - path: "src/services/sessionStats.ts"
      provides: "Token savings accumulation service"
      exports: ["accumulateStats", "StatsDelta", "SessionStats", "SESSION_STATS_PATH", "STATS_TTL_MS", "_resetMutexForTesting"]
    - path: "src/scripts/statusline.mjs"
      provides: "Standalone status line renderer"
      exports: ["formatTokenCount", "readStats", "renderOutput", "IDLE_OUTPUT", "_readStatsFromPath"]
    - path: "src/lib/statusline-script.ts"
      provides: "Embedded script content for init deployment"
      exports: ["STATUSLINE_SCRIPT_CONTENT"]
    - path: "src/lib/config.ts"
      provides: "SESSION_STATS_FILENAME constant"
      contains: "SESSION_STATS_FILENAME"
    - path: "tests/services/sessionStats.test.ts"
      provides: "8 unit tests for sessionStats"
    - path: "tests/scripts/statusline.test.ts"
      provides: "Unit tests for statusline pure functions"
    - path: "tests/scripts/statusline.integration.test.ts"
      provides: "6 integration tests for statusline subprocess"
  key_links:
    - from: "src/services/sessionStats.ts"
      to: "src/lib/config.ts"
      via: "import GLOBAL_CONFIG_DIR, SESSION_STATS_FILENAME"
      pattern: "import.*config\\.js"
    - from: "src/scripts/statusline.mjs"
      to: "~/.brain-cache/session-stats.json"
      via: "readFileSync"
      pattern: "readFileSync.*STATS_PATH"
---

<objective>
Port the sessionStats service, statusline script, and all associated tests from current HEAD onto the v3.0 reset branch.

Purpose: These files form the status line feature — the UX proof that brain-cache saves tokens. They must exist and pass tests before wiring into MCP handlers (Plan 02).

Output: 4 source files + 3 test files, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

NOTE: This phase ports code from current HEAD (master) to the `v3.0-skill-reshape` branch.
The reset branch has only v1.0 core services + incremental indexing.
Key difference: `configLoader.js` may not exist on the reset branch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port sessionStats service and config constant</name>
  <files>src/lib/config.ts, src/services/sessionStats.ts, tests/services/sessionStats.test.ts</files>
  <read_first>
    - src/lib/config.ts (on the reset branch — check if SESSION_STATS_FILENAME already exists)
    - src/services/sessionStats.ts (from current HEAD — reference implementation below)
    - tests/services/sessionStats.test.ts (from current HEAD — reference tests below)
  </read_first>
  <action>
1. **Add SESSION_STATS_FILENAME to config.ts**: If not already present on the reset branch, add `export const SESSION_STATS_FILENAME = "session-stats.json";` to src/lib/config.ts alongside other path constants.

2. **Create src/services/sessionStats.ts**: Port the file from current HEAD with ONE adaptation:
   - The current HEAD version imports `loadUserConfig` from `../services/configLoader.js` to read custom TTL from `~/.brain-cache/config.json`.
   - On the reset branch, `configLoader.ts` may not exist (it was cut as a v2.0+ service).
   - **Adaptation**: Replace the `loadUserConfig` import with an inline config read. In `_doAccumulate`, where it currently does `const config = await loadUserConfig()`, instead do:
     ```typescript
     try {
       const raw = await readFile(join(GLOBAL_CONFIG_DIR, 'config.json'), 'utf-8');
       const config = JSON.parse(raw) as { stats?: { ttlHours?: number } };
       const ttlHours = config.stats?.ttlHours;
       effectiveTtlMs = ttlHours !== undefined ? ttlHours * 60 * 60 * 1000 : STATS_TTL_MS;
     } catch {
       effectiveTtlMs = STATS_TTL_MS;
     }
     ```
   - This removes the configLoader dependency while preserving identical behavior.
   - All other code (mutex, atomic write via rename, types, exports) stays identical.

3. **Create tests/services/sessionStats.test.ts**: Port the test file from current HEAD with ONE adaptation:
   - Replace the `vi.mock('../../src/services/configLoader.js')` with a mock for `node:fs/promises` readFile, or simply mock the config.json file on disk in the temp directory for Test 8.
   - Actually, since sessionStats now reads config.json directly via `readFile`, Test 8 should write a `config.json` file to the temp directory (which is mocked as GLOBAL_CONFIG_DIR) containing `{"stats":{"ttlHours":0.5}}` instead of mocking `loadUserConfig`.
   - All other tests (1-7) stay identical.

Reference implementation (current HEAD src/services/sessionStats.ts):
- Exports: accumulateStats, StatsDelta, SessionStats, SESSION_STATS_PATH, STATS_TTL_MS, _resetMutexForTesting
- Uses module-level mutex (_statsMutex) for serialization
- Atomic write pattern: write to .tmp then rename
- TTL expiry: compare Date.parse(lastUpdatedAt) against effectiveTtlMs
- Fire-and-forget: errors caught and logged via pino childLogger
  </action>
  <verify>
    <automated>cd /workspace && npx vitest run tests/services/sessionStats.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/config.ts exports SESSION_STATS_FILENAME
    - src/services/sessionStats.ts exports accumulateStats, StatsDelta, SessionStats, SESSION_STATS_PATH, STATS_TTL_MS, _resetMutexForTesting
    - sessionStats.ts does NOT import from configLoader.js
    - All 8 sessionStats tests pass (create, accumulate, concurrent, TTL reset, TTL within, no-throw, valid JSON keys, custom TTL from config.json)
  </acceptance_criteria>
  <done>sessionStats service exists with all exports, no configLoader dependency, all 8 tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Port statusline script, embedded content, and tests</name>
  <files>src/scripts/statusline.mjs, src/lib/statusline-script.ts, tests/scripts/statusline.test.ts, tests/scripts/statusline.integration.test.ts</files>
  <read_first>
    - src/scripts/statusline.mjs (from current HEAD — port verbatim)
    - src/lib/statusline-script.ts (from current HEAD — port verbatim)
    - tests/scripts/statusline.test.ts (from current HEAD — port verbatim)
    - tests/scripts/statusline.integration.test.ts (from current HEAD — port verbatim)
  </read_first>
  <action>
1. **Create src/scripts/statusline.mjs**: Port verbatim from current HEAD. This is a standalone ESM script with zero project imports — it reads ~/.brain-cache/session-stats.json directly. No adaptation needed.

   Key exports: formatTokenCount, readStats, renderOutput, IDLE_OUTPUT, _readStatsFromPath
   Stdin/stdout protocol: reads stdin (ignored), writes rendered output to stdout.

2. **Create src/lib/statusline-script.ts**: Port verbatim from current HEAD. This is a string constant containing the full content of statusline.mjs for deployment by the init workflow. The string must exactly match statusline.mjs content.

3. **Create tests/scripts/statusline.test.ts**: Port verbatim from current HEAD. Tests pure functions (formatTokenCount, _readStatsFromPath, renderOutput) with temp directory isolation. No project dependencies to adapt.

4. **Create tests/scripts/statusline.integration.test.ts**: Port verbatim from current HEAD. Runs statusline.mjs as a subprocess with HOME env override to test stdin/stdout protocol. No adaptation needed.

5. **Ensure tests/scripts/ directory exists** on the reset branch. Create if needed.
  </action>
  <verify>
    <automated>cd /workspace && npx vitest run tests/scripts/statusline.test.ts tests/scripts/statusline.integration.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/scripts/statusline.mjs exists with formatTokenCount, readStats, renderOutput exports
    - src/lib/statusline-script.ts exists with STATUSLINE_SCRIPT_CONTENT export
    - STATUSLINE_SCRIPT_CONTENT string matches statusline.mjs file content exactly
    - All statusline unit tests pass (formatTokenCount edge cases, _readStatsFromPath null cases, renderOutput formatting)
    - All 6 integration tests pass (valid stats, no file, expired, malformed, zero estimated, cold-start timing)
  </acceptance_criteria>
  <done>statusline.mjs and statusline-script.ts exist, all unit and integration tests pass</done>
</task>

</tasks>

<verification>
All status line source files exist on the reset branch. All tests pass:
- `npx vitest run tests/services/sessionStats.test.ts` — 8 tests pass
- `npx vitest run tests/scripts/statusline.test.ts` — all unit tests pass
- `npx vitest run tests/scripts/statusline.integration.test.ts` — 6 integration tests pass
- `npm run build` still succeeds (no broken imports)
</verification>

<success_criteria>
- sessionStats.ts accumulates stats with mutex and atomic writes, no configLoader dependency
- statusline.mjs renders savings or idle from session-stats.json
- statusline-script.ts contains embedded script content matching statusline.mjs
- config.ts exports SESSION_STATS_FILENAME
- All 3 test files pass (14+ tests total)
</success_criteria>

<output>
After completion, create `.planning/phases/34-cherry-pick-status-line/34-01-SUMMARY.md`
</output>
